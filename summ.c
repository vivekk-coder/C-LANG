#include<stdio.h>
int main () {
    int a,b;

    printf("enter the no a: ");
    scanf("%d",&a);

    printf("enter the no b: ");
    scanf("%d",&b);

    int sum = a+b;

    printf("the sum of a and b is : %d ",sum);


    return 0;
}